# Open Items Register

The ONLY place where spec questions, deviations, and gaps open or close.
No item closes without a recorded decision (and, where applicable, an ADR
or harness evidence). Traceability between releases is maintained here.

Format: `OI-NNNN | status | opened (phase) | decision-owner | description → decision/evidence`.
Statuses: OPEN · DECIDED · CLOSED (evidence attached) · SUPERSEDED.

## Decisions taken at D0 start (sponsor answers, locked)

| ID | Status | Item | Decision (date: build start) |
|---|---|---|---|
| OI-0001 | DECIDED | v1 vendor priority | **All six in parallel at equal priority** (Cisco IOS/IOS-XE, RouterOS, Junos, FortiOS, Aruba/HP, UniFi). ADR-0001. |
| OI-0002 | DECIDED | Access hardware for Day-0 | **Direct PC connection only**; OOB modeled, default NOT_CONFIGURED. ADR-0004. |
| OI-0003 | DECIDED | Host OS / packaging | **Windows MSI first**. ADR-0008. |
| OI-0004 | DECIDED | LLM runtime | **Cloud API**, signed egress policy + redaction; local Ollama provider shipped as fallback. ADR-0005. |
| OI-0005 | DECIDED | Lab substrate | **Real hardware lab**; no containerlab/GNS3 in v1. ADR-0006. |
| OI-0006 | DECIDED | v1 autonomy ceiling | **M3 Controlled Autonomous**; HUMAN_ONLY set immutable; M4/M5 not releasable. ADR-0007. |

## Technical open items

| ID | Status | Opened | Item | Notes / required resolution |
|---|---|---|---|---|
| OI-0010 | OPEN | D0 | Batfish version pin + feature coverage matrix | Decide at D2 entry; coverage gaps must map to NOT_MODELED states. |
| OI-0011 | OPEN | D0 | CI runner provisioning for Windows + lab bench scheduling | Decide at D1 entry. |
| OI-0012 | OPEN | D0 | UI component library for Tauri frontend | Decide at D6 entry; no impact on control plane. |
| OI-0013 | OPEN | D0 | Runbook authoring format (≥20 deterministic runbooks, §17) | Decide at D5 entry; format must be executable-as-data (steps as typed actions), not free text. |
| OI-0014 | OPEN | D0 | SQLite→PostgreSQL profile switch semantics | Decide if multi-operator deployments requested; single-operator desktop is v1 default. |
| OI-0101 | OPEN | D0 | RouterOS model-specific defaults beyond bridge IP | Fill from lab evidence in D1; profile fields currently `verified:false`. |
| OI-0102 | OPEN | D0 | Cisco IOS-XE per-model factory access facts | Same as above. |
| OI-0103 | OPEN | D0 | Junos factory addressing per platform | Same. |
| OI-0104 | OPEN | D0 | FortiOS per-model default IP confirmation | 192.168.1.99 seed marked unverified. |
| OI-0105 | OPEN | D0 | ArubaOS checkpoint capability per version | Currently UNKNOWN in access profile ⇒ dependent rollbacks BLOCKED until verified. |
| OI-0106 | OPEN | D0 | UniFi controller API compatibility matrix per controller version | D1 API exploration on lab controller. |
| OI-0107 | OPEN | D0 | Day-0 classifier criteria per vendor/version | `defaults_db/day0_classifiers.json` to be seeded from lab factory devices. |
| OI-0110 | CLOSED | D0 | FSM diagram renderings (mermaid) for FSM-1..5 | **Closed:** mermaid sources at `docs/D0/03-state-machines.md.pics/fsm1-device.mmd … fsm5-recovery.mmd`; guard tables in D0-03 remain the normative text. |
| OI-0111 | OPEN | D0 | `defaults_db` platform defaults for reconciliation (D2 input) | Structured in D2; schema drafted with Reconciliation Engine. |
| OI-0112 | CLOSED | D0 | Schemas for Change/Rollback/AutonomyPolicy/GateVerdict objects | **Closed:** `specs/schemas/{change,rollback,autonomy_policy,gate_verdict,decision_readiness,state_transition}.schema.json`; HUMAN_ONLY set un-representable in policy schema by construction (ADR-0007). |
| OI-0113 | OPEN | D0 | Command→field registry generation from allowlists | Implemented in D1 with Collector/Parsers. |
| OI-0114 | OPEN | D0 | Egress policy schema + provider endpoint allowlist file | Drafted with ADR-0005 implementation in D4 agents batch; security-plane core (keys, RBAC) lands D1. |
| OI-0120 | OPEN | D1 | Migrate schema test refs from RefResolver to `referencing` lib | Cosmetic/debt; RefResolver deprecated but functional for draft-07 store. |
| OI-0121 | OPEN | D1 | KeyRegistry → OS keychain binding (Security Plane) | D1 scope is in-memory registry with signing callables; keychain provider arrives with the security-plane batch. |
| OI-0122 | OPEN | D1 | Twin layers Intent/Security/Service/Historical population | Predicate→layer registry seeds Physical/Logical/Operational/Dependency; remaining layers populate with their engines (D2/D5). |
| OI-0123 | CLOSED | D1 | FSM-2/FSM-3 guard tables as code | **Closed (Batch 4):** `fsm/change_fsm.py` (22 transitions, guards 2.1–2.15) + `fsm/rollback_fsm.py` (11 transitions, guards 3.1–3.10); READY preconditions proven 1:1 with `rollback.schema.json` by test. |
| OI-0124 | CLOSED | D1 | FSM-4 (link evidence) + FSM-5 (recovery) guard tables as code | **Closed (Batch 5):** `fsm/link_fsm.py` (15 transitions, guards 4.1–4.9 incl. STALE quarantine) + `fsm/recovery_fsm.py` (27 transitions, guards 5.1–5.6 incl. L5 DESTRUCTIVE human gate & recorded L3 skip per ADR-0004). Docs D0-03 extended with guard 4.9; fsm5 diagram aligned with the normative table. All five FSMs now executable: 93 guarded transitions total. |
| OI-0125 | CLOSED | D1 | Dependency bootstrap not reproducible (sandbox rebuild dropped cryptography) | **Closed:** `requirements.txt` + README run instructions; policy = dependency enters only when its engine lands. |
| OI-0131 | OPEN | D1 | Junos golden fixture is synthetic-representative | Replace with real lab capture before parser is used for DEVICE_IDENTITY claims on Junos hardware. |
| OI-0132 | OPEN | D1 | FortiOS golden fixture is synthetic-representative | Same as OI-0131 for FortiGate. |
| OI-0133 | OPEN | D1 | ArubaOS golden fixture is synthetic-representative | Same as OI-0131 for Aruba controllers. |
| OI-0134 | OPEN | D1 | UniFi golden fixture is synthetic-representative | Same as OI-0131 for the local UniFi controller; verify against controller version in lab. |
| OI-0140 | OPEN | D2 | Capability matrix operational fields (configure/validate/rollback/operational_test) are UNKNOWN | Grow per lab evidence only (matrix growth_rule); every upgrade YES/PARTIAL requires a lab-evidence bundle + register entry. |
| OI-0141 | OPEN | D2 | ArubaOS checkpoint_rollback capability UNKNOWN | Tied to OI-0105; until lab-verified, ArubaOS changes depending on checkpoint rollback stay BLOCKED (T2). |
| OI-0142 | OPEN | D2 | IPAM DHCPv6/SLAAC pool planning NOT_MODELED in v1 | E07 raises NOT_SUPPORTED for IPv6 pool planning; model lands when a v6 service design requirement is registered. |
| OI-0143 | CLOSED | D2 | Config IR (E09) + Validation Fabric (E10) pipeline | **Closed (Batch 2):** `engines/config_ir.py` (reversibility tags P0-18 incl. IRREVERSIBLE justification rule, gate-class derivation) + `engines/validation_fabric.py` (nine stages, BLOCKED-outranks-FAIL algebra, NOT_MODELED preflight recorded per L13, entity guard against Twin). 21 new tests. |
| OI-0144 | CLOSED | D2 | Intent Compiler (E08) + zone×zone policy matrix + blocking questions | Delivered `engines/intent_compiler.py` + 13 tests; §12 guest-isolation golden ships as `test_intent_compiler.py` (4 mandated decisions, precedence-ordered carve-outs, complete 4×4 matrix = 17 rules with 8 DEFAULT_DENY, zero addressing fields in `PolicyRule`). Outcome: COMPILED/BLOCKED states, blocking questions raised in one pass (root question suppresses derivatives), service bring-up order delegated to ServiceGraph. |
| OI-0145 | CLOSED | D2 | Service Dependency Graph (P1-34) structured data + depends_on/provides registry | Delivered `specs/data/defaults_db/service_dependencies.json` (14 services) + `engines/service_graph.py` + 10 tests; deterministic Kahn ordering, transitive closures, cycle/dangling-dep fail-fast at construction. Consumed by Intent Compiler (`order_for`). |
| OI-0146 | CLOSED | D2 | Failure as frozen-dataclass Exception | **Closed:** rewritten as plain Exception subclass (exception machinery must set `__traceback__`); public API unchanged, all prior failure tests pass unmodified. |
| OI-0147 | CLOSED | D2 | Preflight Engine (E11) supplying the fabric's `PreflightFn` contract | Delivered `engines/preflight.py` + 12 tests. Per-node assessment against Twin-evidenced identity + AdapterRegistry: NO_ADAPTER⇒NOT_MODELED, ENTITY_ABSENT/IDENTITY_INCOMPLETE/VENDOR_OS_MISMATCH/CAPABILITY_NOT_SUPPORTED⇒MODEL_INCOMPLETE, CAPABILITY_UNKNOWN⇒PARTIALLY_MODELED, SUPPORTED⇒MODELED; worst-first aggregate in exact `PreflightStatus` vocabulary (L13: NOT_MODELED dominates). Zero I/O. Fabric INFO text updated (no more "lands with E11"). |
| OI-0149 | CLOSED | D3 | Dependency DAG Engine (E12), FSM-2 guard 2.2 | Delivered `engines/dependency_dag.py` + 11 tests. Total deterministic topological order (Kahn, frontier sorted by mgmt-first then node_id); typed fail-fast: UNKNOWN_DEPENDS_ON, UNSATISFIED_REQUIRES, DUPLICATE_PROVIDES, CONFLICT, BLOCKS_VIOLATION, DEPENDENCY_CYCLE, SERVICE_DEP_UNMET. Permanent constraints: mgmt-plane tie-break first; controller/underlay/AAA-NTP-PKI ordering enforced through declared requires/provides + P1-34 `service:` token namespace (never inferred silently). |
| OI-0150 | CLOSED | D3 | Blast Radius Engine (E13), FSM-2 guard 2.3 | Delivered `engines/blast_radius.py` + 10 tests. Channels: direct entities, IR-internal fixed-point token/target propagation, P1-34 service dependents; twin-link propagation honestly NOT_MODELED until endpoint fields exist (gap recorded in report). Risk class = gate class escalated LOW→HIGH past absolute blast threshold (policy fact, T3); worst-case recorded deterministically. |
| OI-0152 | CLOSED | D3 | Autonomy Authority Engine (E14), D0-02 DECIDE cell "Autonomy decision" | Delivered `engines/autonomy.py` + 15 tests. Ceiling locked at M3 Controlled Autonomous (OI-0006); higher ceilings refuse construction; lower ceilings never auto-execute. Gate verdicts: LOW_RISK⇒AUTO_EXECUTE@M3; MFA gates⇒APPROVAL_REQUIRED satisfied only by RBAC identity + verified MFA; HUMAN_ONLY gates human-decided in every mode (L06) with MFA completeness enforced; NOT_MODELED terminal even with approval (L13). `DecisionReadiness` §13 conjuncts fail-closed (UNKNOWN counts false). |
| OI-0153 | CLOSED | D3 | Change Engine (E15) driving FSM-2 decision spine 2.1→2.4 | Delivered `engines/change_engine.py` + 12 tests. Fabric→DAG→Blast→Autonomy wired to real guards with exact evidence-kind strings; typed stops at DRAFT (fabric), VALIDATED (DAG service gaps — defense in depth), RISK_ASSESSED (approval/readiness gaps); approval/MFA evidence emitted only when facts hold (T1); PREPARED stops honestly at AUTHORIZED (guards 2.5+ belong to execution batches); veto path to REJECTED validated. |
| OI-0155 | CLOSED | D3 | Rollback Engine (E16) driving FSM-3 per ADR-0009 | Delivered `engines/rollback_engine.py` + 11 tests. Plans from IR against the shipped Recovery Matrix (destructive mechanisms excluded per §9; PRIMARY-marked mechanism preferred deterministically; IRREVERSIBLE nodes ⇒ infeasible, never approximated). Configure fires 3.2→3.3→3.4 with one evidence item per §14 precondition; artifact sha256 RECOMPUTED at configure AND trigger (tamper ⇒ typed refusal). Trigger is legal from ARMED only — CONFIRMED seals the success path (D0-03 3.6/3.7); complete routes to ROLLBACK_FAILED with named conjuncts (L13). |
| OI-0157 | CLOSED | D3 | Failure Orchestrator (E18), FSM-2 guard 2.8 | Delivered `engines/failure_orchestrator.py` + 14 tests. Deterministic fail-safe algebra over the five decisions (CONTINUE/ISOLATE/PARTIAL_ROLLBACK/FULL_ROLLBACK/HALT): MANUAL_REQUIRED & FATAL always HALT; irreversible/destructive applied state never auto-rolled-back; RETRYABLE within budget CONTINUEs; mgmt-path impact ISOLATEs; PARTIAL class over replaceable state ⇒ PARTIAL_ROLLBACK with failure's own n/N counts (T4); tail-node vs stage-level split; nothing applied ⇒ HALT NO_STATE_TO_ROLLBACK (no invented rollback). Evidence pair matches guard 2.8 kinds exactly. |
| OI-0158 | CLOSED | D3 | Verification Engine (E19), FSM-2 guards 2.9/2.10 | Delivered `engines/verification.py` + 10 tests. Mandatory test matrix derived deterministically from the compiled intent: one connectivity expectation per zone×zone pair resolved by rule precedence (carve-out beats blanket deny) + SERVICE_UP per required service; missing/unplanned results are typed BLOCKED (never PASS, T1); guard-2.10 evidence shapes: all-pass OR per-non-pass human acceptance. |
| OI-0160 | CLOSED | D4 | Agent Boundary Guard (agents/guard.py), L01/L02/L11/L17 | Delivered `agents/guard.py` + 13 tests. Four checks, all reported in one deterministic verdict: (1) agent_output.schema.json Draft-07 validation — Intent Object is the only accepted shape (L17); (2) Entity Guard — every target entity must pre-exist in the Twin or the payload is quarantined and `entity_hallucinations` incremented (T5); (3) raw-command detection with prefixes DERIVED from the six vendor allowlists (quarantine and execution stay in lockstep; no hardcoded vendor knowledge); (4) credential-pattern scan ⇒ `credential_exposure` + quarantine (L11). |
| OI-0161 | CLOSED | D4 | Agent Context Builder (agents/context.py), L11 | Delivered `agents/context.py` (covered by guard test file). Facts emitted in sorted deterministic order; every secret-bearing predicate (frozen list) is REDACTED — presence recorded, value never emitted; absent entities yield no facts, never invented (L01); `has_secret_leak()` self-check. |
| OI-0163 | CLOSED | D4 | Claim/Relevance Verifier (D0-04 §5 Relevance Guard, T1) | Delivered `ledger/claim_verifier.py` + 9 tests. Three rules enforced per evidence item with full sorted reporting: (1) same device via the Observation→RawArtifact→Event chain (non-DEVICE subjects require a single consistent origin, cross-device never inferred); (2) command→field registry agreement — Event.command_or_op must equal the producing parser's command_ref AND the observation must be of the claimed field; (3) parse_status=OK only (MISSING/PARSE_FAILED proves nothing, L01). Superseded observations carry no relevance (D0-04 §1). Also closed the ledger read gap: `observations()` / `raw_artifacts()` accessors added to `LedgerStore`. |
| OI-0164 | CLOSED | D4 | Link Evidence Engine driving FSM-4 per link (D0-03 §FSM-4, §10) | Delivered `engines/link_evidence.py` + 12 tests. One guarded machine per link id; every rung fires exactly the evidence kind its guard demands; passive ceiling (PROBABLE) enforced by the machine; CONFIRMED requires proven absence of intermediary (4.6); PHYSICAL_PATH_VERIFIED only via gated active proof or human confirmation (4.7); CONFLICTING terminal for automation (4.3); STALE→UNKNOWN only through recorded re-evaluation (4.9). Empty evidence ids and arity mismatches are typed refusals. |
| OI-0162 | CLOSED | D4 | Embedded copies of agent_output schema + 6 allowlists | Mirrored under `agents/data/`; seven new byte-compare pairs in `test_data_sync.py`. |
| OI-0159 | CLOSED | D3 | Temporary Resource Manager (cleanup), FSM-2 guard 2.12 | Delivered `engines/cleanup.py` + 9 tests. Register-before-apply; verify against observed removals; untracked removals get no credit; preservation requires identity + reason; each leak increments `cleanup_leaks` (T5) and refuses CLEANED evidence. |
| OI-0156 | CLOSED | D3 | Recovery Engine (E17) driving FSM-5 hierarchy + OOB | Delivered `engines/recovery_engine.py` + 11 tests. Attempt outcomes are caller-supplied — the engine never fabricates them (T2: ATTEMPT_RESULT_REQUIRED). Matrix skips evidence-driven: NOT_SUPPORTED ⇒ recorded skip; L3/OOB UNKNOWN ⇒ NOT_CONFIGURED per ADR-0004 v1 default. L4→L5 has its own gate-checked path requiring a recorded human decision (5.4/L06); L6/L7 emit HUMAN_TASK and wait (5.5); exhausted paths end in LOST (5.6), recorded. |
| OI-0154 | CLOSED | D3 | `policy.authority.veto()` listed in D0-02 derived artifact but absent since D1 | Gap discovered while building E15. Implemented `VetoRecord` + `veto()` with mandatory identity and reason (D0-02 rule 5); anonymous or unreasoned veto raises. |
| OI-0151 | CLOSED | D3 | `gate_class` ignored MGMT_FEATURES vocabulary | Discovered while building E13: `ConfigIR.touches_management_plane()` checked only explicit flags. Fixed: known mgmt features (frozen list, moved to `config_ir.py` as canonical owner) escalate deterministically; validation_fabric now imports the list. No prior test regressed. |
| OI-0148 | CLOSED | D2 | Reconciliation baseline + typed drift (D2 scope of E22) | Delivered `reconcile/engine.py` + 15 tests: immutable baselines (`MappingProxyType`, `baseline_id` satisfies FSM-1 guard 1.8), deterministic ADDED/REMOVED/MODIFIED records, classification MANAGED / DEFAULT_RESTORED (known platform default only — UNKNOWN defaults never qualify, T2) / UNMANAGED_DRIFT; unmanaged drift increments `unauthorized_changes`; remediation proposed as structured hint codes only, never command text (L11). |

| OI-0182 | OPEN | D5-cap | Management-path sessions (SSH transport batch) | mgmt sessions to discovered neighbors are typed-refused today (ADR-0004 direct-connect). Neighbors stay discovered-unreached — never assumed managed. |
| OI-0183 | OPEN | D5-cap | A1–A5 PROPOSE→DECIDE wiring over the authority table | Agent plane proposes Intent Objects only (guard L17); DECIDE wiring scheduled with the LLM provider batch. |
| OI-0173 | OPEN | D5-cap | Normalizer (E03) for canonical identity predicates (vendor/os/os_version) | Crawl claims carry parser-field predicates (T1-clean); deriving canonical identity needs a Normalizer with its own evidence chain. Downstream reads crawl-report identity meanwhile. Referenced in `engines/claim_factory.py`, `engines/discovery_crawl.py`. |
| OI-0184 | CLOSED | D5-cap | CONFIG allowlist classes non-empty for all six vendors — measured | **Closed (Phase Y):** Measured CONFIG_REVERSIBLE + CONFIG_HIGH_RISK entry counts per vendor: Cisco IOS-XE 28+8, Junos 7+3, RouterOS 14+4, FortiOS 10+13, ArubaOS 8+3, UniFi 4+3. All six carry rollback declarations on ≥70% of their CONFIG entries (Cisco, Junos, RouterOS, UniFi 100%; FortiOS 17/23 because 6 `config <section>` entries change no state and declare no inverse — correct by design). The register entry's original premise ("CONFIG classes empty") was true at D5 write-time but has been filled by the shipped `lab-verified-v1` allowlist data since then. The EXECUTION_GATE still produces STAGED previews with lineage — that is its design, not a consequence of empty data. |
| OI-0186 | CLOSED | Phase Y | Adapter registry was never constructed in shipped code | **Closed:** `adapters/bootstrap.py` delivers `default_registry()` — one adapter per vendor family, built from allowlists + parser catalog data. The only prior constructions were in `tests/`; in the installed product `AdapterRegistry.resolve()` could only ever return `None`. `test_shipped_capability_is_real.py` asserts all six families resolve an adapter; an unknown vendor resolves to nothing (never a guess). verify_install.py step [8/8] asserts the same. |
| OI-0187 | CLOSED | Phase Y | Config capture command hardcoded to Cisco for all vendors | **Closed:** `access/executor.py` — both `_read_running_config()` and `_read_running_hash()` previously sent the literal `"show running-config"` to every device. On Junos/RouterOS/FortiOS/Aruba/UniFi that is not the configuration; the `except Exception` swallowed the failure and verification silently degraded to "state change not confirmed". Resolution: per-family purpose-based lookup (`config_capture_command()`), with `show running-config` as backward-compatible fallback only when the family's own allowlist registers it as READ_ONLY. Cisco resolves `show running-config`; Junos resolves `show configuration | display set`; four families resolve `None` (they have no configuration-dump command their allowlist permits) and the executor reports `CONFIG_CAPTURE_NOT_MODELED` instead of silently passing. |
| OI-0188 | CLOSED | Phase Y | RouterOS MNDP neighbor parser vocabulary mismatch — silent evidence loss | **Closed:** `parsers/neighbor_parsers.py` — `RouterOsIpNeighborPrintParser.command_ref` was `/ip neighbor/print` (space form) while the allowlist registers `/ip/neighbor/print` (path form, matching every other RouterOS template). `DiscoveryCrawl.plan_for()` intersects catalog ∩ allowlist on the exact string, so the MNDP parser was silently never planned and RouterOS neighbor evidence was never collected. Fixed: `command_ref` aligned to the family's own allowlisted spelling. The golden fixture `ip_neighbor_print.expected.json` updated accordingly. `test_shipped_capability_is_real.py::test_routeros_neighbour_evidence_is_actually_planned` guards the regression. |
| OI-0189 | CLOSED | Phase Y | Plan gap invisibility — dropped commands had no typed report | **Closed:** `CommandStatus.PARSER_MISSING` was declared and never produced; a parser whose `command_ref` was not allowlisted was silently dropped by `plan_for()`. `DiscoveryCrawlEngine.plan_gaps_for()` now answers the complement of `plan_for()`: "what can this family parse but not ask for, and why" — typed reasons `NOT_ALLOWLISTED` (no matching template) and `WRONG_CLASS:<class>` (template exists but not READ_ONLY). Reported per-device and de-duplicated in `totals["plan_gaps"]`. `test_shipped_capability_is_real.py` asserts zero gaps for all six shipped families and asserts the detector catches both reasons on a crafted allowlist. |
| OI-0190 | CLOSED | Phase Y | `paramiko` undeclared as a dependency despite being exercised | **Closed:** `requirements.txt` and `pyproject.toml [project.optional-dependencies] hardware` and `all` now declare `paramiko>=3.4` with an explanation of why it is an explicit dependency (banner evidence calls into the Paramiko transport directly, not just through Netmiko). `test_ssh_transport_reads_the_device_banner.py` now imports paramiko gracefully and skips when absent, so a bare install without the `hardware` extra does not break test collection. |
| OI-0191 | CLOSED | Phase Y | `verify_install.py` e2e check misaligned with orchestrator question flow | **Closed:** Steps [7/8] replaced a hand-written 6-answer literal with `answer_script(access_retry='n', intent='2')`, the single source of truth for the orchestrator's question order. The old list (`['y', '2', 'seed-01', 'ISP fiber DHCP', 'STANDARD', '+25% in 12 months']`) silently slid out of alignment when the orchestrator grew the access-retry, DNS and typed-BOND questions: `'seed-01'` landed on the intent question, producing `BLOCKED-BLOCKED` while the engine was behaving correctly. verify_install now exits 0. Step [8/8] added: asserts every vendor family resolves an adapter. |
| OI-0170 | OPEN | D5-cap | Cisco/Junos/RouterOS/Aruba/UniFi neighbor fixtures are synthetic-representative | Same policy as OI-0131..0134: replace with lab captures before DEVICE_IDENTITY claims on hardware. |
| OI-0172 | OPEN | D5-cap | FortiOS LLDP fixture shape pending lab capture | Parser tolerant to key spelling; fixture marked synthetic-representative. |
| OI-0175 | CLOSED | D5-cap | Port-name normalization (D0-08 §4) absent | **Closed:** `parsers/portnames.py` (Cisco long≡short e.g. GigabitEthernet1/0/1≡Gi1/0/1, Junos unit fold ge-0/0/1.0≡ge-0/0/1; unmapped stays raw). Wired into evidence fusion + bidirectional matching. |
| OI-0176 | CLOSED | D5-cap | LLDP+CDP neighbor tables overwrote each other in the Twin | **Closed:** protocol-suffixed predicates (`neighbor_table_lldp/cdp/mndp`) + deterministic fusion in the crawl (fixed key set, LLDP→CDP→MNDP priority, later sources only fill gaps). |
| OI-0177 | CLOSED | D5-cap | Infrastructure port could be assigned as end-user access | **Closed:** any port carrying ANY neighbor evidence is infrastructure-reserved in the design engine (never access). |
| OI-0178 | CLOSED | D5-cap | IPAM per-prefix index allocation could overlap across prefix lengths | **Closed:** zone packing rewritten as first-fit aligned cursor over the site block (coarsest-first; overlap impossible by construction; exhaustion ⇒ SITE_BLOCK_EXHAUSTED typed stop). |
| OI-0180 | DECIDED | D5-cap | Simulated fabric as ADDITIONAL evaluation substrate | `tests/support/simfabric.py` — deterministic scripted consoles; registered here; the real-hardware lab gate OI-0005 stays OPEN and untouched. |
| OI-0181 | DECIDED | D5-cap | AUTOPILOT as 7th FSM label in StateTransition | Run-machine transitions for capstone phases; validator extended (recorded deviation DEV-0002). |
| OI-0185 | CLOSED | D4 (D5-cap exit) | Evaluation Harness full (T5 report) | **Closed:** `harness/runner.py` — known-answer scenarios replayed twice (instability = FAIL), crashes typed, NOT_TESTED visible, gate-eligible T5 counters computed from runs (no attestation), FI-detection scenarios with expected non-zero counters (`fi:ledger-tamper` proves chain verification catches forgery; RELEASE GATE: PASS at 4/4). PROPOSE→DECIDE remainder tracked as OI-0183. |

## Deviations from master specification

| ID | Phase | Deviation | Cause / justification |
|---|---|---|---|
| DEV-0001 | D1 (Batch 5) | Added guard **4.9** (STALE → UNKNOWN) to the FSM-4 table in `docs/D0/03-state-machines.md` | The state set of §7 includes STALE but the §FSM-4 table defined no exit from it; the D0 diagram (a D0 deliverable of this build) already showed the re-evaluation edge. Codified as a numbered guard rather than an implicit behavior (L03). No semantic conflict with §7/§10. |

Any future deviation is logged here with cause before it is implemented (per §24.3).

| DEV-0002 | D5-capstone | `StateTransition.fsm` validator extended with label `AUTOPILOT` | The capstone run-machine (BOND→…→REPORT) records its transitions in the same append-only store; the label is new data, no semantic change to FSM-1..5. Codified in code + this register (L03). |
