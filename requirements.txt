# Runtime dependencies.
# Pinned lower bounds per ADR-0001; everything listed is required for v1.
pydantic>=2.5
cryptography>=42.0
jsonschema>=4.20

# Console transports (ADR-0008: bundled in the MSI)
pyserial>=3.5            # physical console (direct-connect, the v1 path)
netmiko>=4.3             # SSH console/mgmt sessions (access/ssh_transport.py)
# SSH banner evidence. ``SSHConsoleTransport.banner()`` reads the connect
# banner through the Paramiko transport that Netmiko holds underneath
# (``remote_conn.get_transport().get_banner()``), and the management session
# cross-checks that banner against the dialect it chose before it will run a
# single command. Netmiko pulls Paramiko in transitively, so it has always
# been *installed* — but it was never *declared*, which meant the dependency
# the banner evidence rests on could change or vanish under a resolver that
# reads only this file. Declared here and in the ``hardware`` extra.
paramiko>=3.4
# telnetlib is stdlib on Py ≤3.12, removed in 3.13+ (PEP 594)

# Web UI / API
fastapi>=0.115           # REST + WebSocket surface (optional, only for `webui` subcommand)
uvicorn[standard]>=0.30  # ASGI server (required when fastapi is installed)
httpx<0.28               # required by Starlette TestClient for the web tests

# Reporting
# (no new deps: HTML/JSON report writers use only the stdlib)

# Configuration formats (each is optional; the loader degrades with typed errors)
pyyaml>=6.0              # YAML config files
# tomli>=2.0             # TOML on Python <3.11 (stdlib tomllib from 3.11+)

# Development / harness
pytest>=8.0
pytest-asyncio>=0.23     # for anyio-based async tests (used by FastAPI TestClient)
